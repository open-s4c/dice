name: Test Dice dependants against the current branch
on:
  push:
    branches:
    - main
    tags:
    - '**'
  pull_request:

env:
  OWNER: open-s4c

jobs:
  run-dependant-actions:
    strategy:
      fail-fast: false
      matrix:
        dependant: [dice-rs]
    runs-on: ubuntu-latest
    permissions:
      checks: write
    env:
      CHECK_NAME: ${{ matrix.dependant }}-${{ github.sha }}-${{ github.run_attempt }}
    steps:
      - name: Trigger ${{ matrix.dependant }} workflow
        uses: peter-evans/repository-dispatch@v3
        with:
          token: ${{ secrets.PAT }}
          repository: ${{ env.OWNER }}/${{ matrix.dependant }}
          event-type: test-dice
          client-payload: >
            {
              "sha": "${{ github.sha }}",
              "check_name": "${{ env.CHECK_NAME }}"
            }
      - name: Create empty check
        uses: LouisBrunner/checks-action@v2.0.0
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          name: ${{ env.CHECK_NAME }}
          status: in_progress
      - name: Wait for ${{ matrix.dependant }} result
        uses: lewagon/wait-on-check-action@v1.4.1
        with:
          ref: ${{ github.sha }}
          check-name: ${{ env.CHECK_NAME }}
          repo-token: ${{ secrets.PAT }}
          wait-interval: 10
