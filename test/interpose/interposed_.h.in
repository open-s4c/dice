$_dl; //
$_dl; // Copyright (C) 2025 Huawei Technologies Co., Ltd.
$_dl; // SPDX-License-Identifier: 0BSD
$_dl; //
$_dl;
$_dl; // -----------------------------------------------------------------------
$_dl; // malloc and co
$_dl; // -----------------------------------------------------------------------
$_map(MM_RETURN_malloc, void *);
$_map(MM_PARAMS_malloc, size_t size);
$_map(MM_ARGS_malloc, size);
$_map(MM_RETURN_calloc, void *);
$_map(MM_PARAMS_calloc, size_t number, size_t size);
$_map(MM_ARGS_calloc, number, size);
$_map(MM_RETURN_realloc, void *);
$_map(MM_PARAMS_realloc, void *ptr, size_t size);
$_map(MM_ARGS_realloc, ptr, size);
$_map(MM_RETURN_free, void);
$_map(MM_PARAMS_free, void *ptr);
$_map(MM_ARGS_free, ptr);
$_map(MM_RETURN_posix_memalign, int);
$_map(MM_PARAMS_posix_memalign, void **ptr, size_t alignment, size_t size);
$_map(MM_ARGS_posix_memalign, ptr, alignment, size);
$_map(MM_RETURN_aligned_alloc, void *);
$_map(MM_PARAMS_aligned_alloc, size_t alignment, size_t size);
$_map(MM_ARGS_aligned_alloc, alignment, size);
$_dl; // -----------------------------------------------------------------------
$_dl; // mman
$_dl; // -----------------------------------------------------------------------
$_map(MM_RETURN_mmap, void *);
$_map(MM_PARAMS_mmap, void *addr, size_t length, int prot, int flags, int fd, off_t offset);
$_map(MM_ARGS_mmap, addr, length, prot, flags, fd, offset);
$_map(MM_RETURN_munmap, int);
$_map(MM_PARAMS_munmap, void *addr, size_t length);
$_map(MM_ARGS_munmap, addr, length);
$_dl; // -----------------------------------------------------------------------
$_dl; // pthread_create
$_dl; // -----------------------------------------------------------------------
$_map(MM_RETURN_pthread_exit, void);
$_map(MM_PARAMS_pthread_exit, void *ptr);
$_map(MM_ARGS_pthread_exit, ptr);
$_map(MM_RETURN_pthread_create, int);
$_map(MM_PARAMS_pthread_create, pthread_t *thread, const pthread_attr_t *attr, void *(*run)(void *), void *arg);
$_map(MM_ARGS_pthread_create, thread, attr, run, arg);
$_map(MM_RETURN_pthread_join, int);
$_map(MM_PARAMS_pthread_join, pthread_t thread, void **ptr);
$_map(MM_ARGS_pthread_join, thread, ptr);
$_dl; // -----------------------------------------------------------------------
$_dl; // mutex
$_dl; // -----------------------------------------------------------------------
$_map(MM_RETURN_pthread_mutex_lock, int);
$_map(MM_PARAMS_pthread_mutex_lock, pthread_mutex_t *mutex);
$_map(MM_ARGS_pthread_mutex_lock, mutex);
$_map(MM_RETURN_pthread_mutex_unlock, int);
$_map(MM_PARAMS_pthread_mutex_unlock, pthread_mutex_t *mutex);
$_map(MM_ARGS_pthread_mutex_unlock, mutex);
$_map(MM_RETURN_pthread_mutex_trylock, int);
$_map(MM_PARAMS_pthread_mutex_trylock, pthread_mutex_t *mutex);
$_map(MM_ARGS_pthread_mutex_trylock, mutex);
$_map(MM_RETURN_pthread_mutex_timedlock, int);
$_map(MM_PARAMS_pthread_mutex_timedlock, pthread_mutex_t *mutex, const struct timespec *timeout);
$_map(MM_ARGS_pthread_mutex_timedlock, mutex, timeout);
$_map(MM_RETURN_pthread_mutex_clocklock, int);
$_map(MM_PARAMS_pthread_mutex_clocklock, pthread_mutex_t *mutex, clockid_t clock_id, const struct timespec *abstime);
$_map(MM_ARGS_pthread_mutex_clocklock, mutex, clock_id, abstime);
$_dl; // -----------------------------------------------------------------------
$_dl; // rwlock
$_dl; // -----------------------------------------------------------------------
$_map(MM_RETURN_pthread_rwlock_rdlock, int);
$_map(MM_PARAMS_pthread_rwlock_rdlock, pthread_rwlock_t *lock);
$_map(MM_ARGS_pthread_rwlock_rdlock, lock);
$_map(MM_RETURN_pthread_rwlock_tryrdlock, int);
$_map(MM_PARAMS_pthread_rwlock_tryrdlock, pthread_rwlock_t *lock);
$_map(MM_ARGS_pthread_rwlock_tryrdlock, lock);
$_map(MM_RETURN_pthread_rwlock_timedrdlock, int);
$_map(MM_PARAMS_pthread_rwlock_timedrdlock, pthread_rwlock_t *lock, const struct timespec *abstime);
$_map(MM_ARGS_pthread_rwlock_timedrdlock, lock, abstime);
$_map(MM_RETURN_pthread_rwlock_wrlock, int);
$_map(MM_PARAMS_pthread_rwlock_wrlock, pthread_rwlock_t *lock);
$_map(MM_ARGS_pthread_rwlock_wrlock, lock);
$_map(MM_RETURN_pthread_rwlock_timedwrlock, int);
$_map(MM_PARAMS_pthread_rwlock_timedwrlock, pthread_rwlock_t *lock, const struct timespec *abstime);
$_map(MM_ARGS_pthread_rwlock_timedwrlock, lock, abstime);
$_map(MM_RETURN_pthread_rwlock_trywrlock, int);
$_map(MM_PARAMS_pthread_rwlock_trywrlock, pthread_rwlock_t *lock);
$_map(MM_ARGS_pthread_rwlock_trywrlock, lock);
$_map(MM_RETURN_pthread_rwlock_unlock, int);
$_map(MM_PARAMS_pthread_rwlock_unlock, pthread_rwlock_t *lock);
$_map(MM_ARGS_pthread_rwlock_unlock, lock);
$_dl; // -----------------------------------------------------------------------
$_dl; // spinlock
$_dl; // -----------------------------------------------------------------------
$_map(MM_RETURN_pthread_spin_lock, int);
$_map(MM_PARAMS_pthread_spin_lock, pthread_spinlock_t *lock);
$_map(MM_ARGS_pthread_spin_lock, lock);
$_map(MM_RETURN_pthread_spin_trylock, int);
$_map(MM_PARAMS_pthread_spin_trylock, pthread_spinlock_t *lock);
$_map(MM_ARGS_pthread_spin_trylock, lock);
$_map(MM_RETURN_pthread_spin_unlock, int);
$_map(MM_PARAMS_pthread_spin_unlock, pthread_spinlock_t *lock);
$_map(MM_ARGS_pthread_spin_unlock, lock);
$_dl; // -----------------------------------------------------------------------
$_dl; // cond
$_dl; // -----------------------------------------------------------------------
$_map(MM_RETURN_pthread_cond_wait, int);
$_map(MM_PARAMS_pthread_cond_wait, pthread_cond_t *cond, pthread_mutex_t *mutex);
$_map(MM_ARGS_pthread_cond_wait, cond, mutex);
$_map(MM_RETURN_pthread_cond_signal, int);
$_map(MM_PARAMS_pthread_cond_signal, pthread_cond_t *cond);
$_map(MM_ARGS_pthread_cond_signal, cond);
$_map(MM_RETURN_pthread_cond_broadcast, int);
$_map(MM_PARAMS_pthread_cond_broadcast, pthread_cond_t *cond);
$_map(MM_ARGS_pthread_cond_broadcast, cond);
$_map(MM_RETURN_pthread_cond_timedwait, int);
$_map(MM_PARAMS_pthread_cond_timedwait, pthread_cond_t *cond, pthread_mutex_t *mutex, const struct timespec *abstime);
$_map(MM_ARGS_pthread_cond_timedwait, cond, mutex, abstime);
$_map(MM_RETURN_pthread_cond_clockwait, int);
$_map(MM_PARAMS_pthread_cond_clockwait, pthread_cond_t *cond, pthread_mutex_t *mutex, clockid_t clock_id, const struct timespec *abstime);
$_map(MM_ARGS_pthread_cond_clockwait, cond, mutex, clock_id, abstime);
$_dl; // -----------------------------------------------------------------------
$_dl; // semaphore
$_dl; // -----------------------------------------------------------------------
$_map(MM_RETURN_sem_post, int);
$_map(MM_PARAMS_sem_post, sem_t *sem);
$_map(MM_ARGS_sem_post, sem);
$_map(MM_RETURN_sem_wait, int);
$_map(MM_PARAMS_sem_wait, sem_t *sem);
$_map(MM_ARGS_sem_wait, sem);
$_map(MM_RETURN_sem_trywait, int);
$_map(MM_PARAMS_sem_trywait, sem_t *sem);
$_map(MM_ARGS_sem_trywait, sem);
$_map(MM_RETURN_sem_timedwait, int);
$_map(MM_PARAMS_sem_timedwait, sem_t *sem, const struct timespec *abstime);
$_map(MM_ARGS_sem_timedwait, sem, abstime);
$_dl; // -----------------------------------------------------------------------
$_dl; // cxa
$_dl; // -----------------------------------------------------------------------
$_map(MM_RETURN___cxa_guard_acquire, int);
$_map(MM_PARAMS___cxa_guard_acquire, void *addr);
$_map(MM_ARGS___cxa_guard_acquire, addr);
$_map(MM_RETURN___cxa_guard_release, int);
$_map(MM_PARAMS___cxa_guard_release, void *addr);
$_map(MM_ARGS___cxa_guard_release, addr);
$_map(MM_RETURN___cxa_guard_abort, void);
$_map(MM_PARAMS___cxa_guard_abort, void *addr);
$_map(MM_ARGS___cxa_guard_abort, addr);
$_dl; // -----------------------------------------------------------------------
$_dl; // annotate_rw_lock
$_dl; // -----------------------------------------------------------------------
$_map(MM_RETURN_AnnotateRWLockCreate,void);
$_map(MM_PARAMS_AnnotateRWLockCreate, const char *file, int line, const volatile void *lock);
$_map(MM_ARGS_AnnotateRWLockCreate,file,line,lock);
$_map(MM_RETURN_AnnotateRWLockDestroy,void);
$_map(MM_PARAMS_AnnotateRWLockDestroy, const char *file, int line, const volatile void *lock);
$_map(MM_ARGS_AnnotateRWLockDestroy,file,line,lock);
$_map(MM_RETURN_AnnotateRWLockAcquired,void);
$_map(MM_PARAMS_AnnotateRWLockAcquired, const char *file, int line, const volatile void *lock , long is_w);
$_map(MM_ARGS_AnnotateRWLockAcquired, file, line, lock, is_w);
$_map(MM_RETURN_AnnotateRWLockReleased,void);
$_map(MM_PARAMS_AnnotateRWLockReleased, const char *file, int line, const volatile void *lock , long is_w);
$_map(MM_ARGS_AnnotateRWLockReleased, file, line, lock, is_w);
$_dl; // -----------------------------------------------------------------------
$_dl; // memcpy
$_dl; // -----------------------------------------------------------------------
$_map(MM_RETURN_memcpy, void *);
$_map(MM_PARAMS_memcpy, void *dest ,const void *src ,size_t num);
$_map(MM_ARGS_memcpy,  dest , src , num);
$_dl; // -----------------------------------------------------------------------
$_dl; // memmove
$_dl; // -----------------------------------------------------------------------
$_map(MM_RETURN_memmove, void *);
$_map(MM_PARAMS_memmove, void *dest ,const void *src ,size_t count);
$_map(MM_ARGS_memmove,  dest , src , count);
$_dl; // -----------------------------------------------------------------------
$_dl; // memset
$_dl; // -----------------------------------------------------------------------
$_map(MM_RETURN_memset, void *);
$_map(MM_PARAMS_memset, void *ptr, int value, size_t num);
$_map(MM_ARGS_memset, ptr, value, num);
$_dl; // -----------------------------------------------------------------------
$_dl; // epoll
$_dl; // -----------------------------------------------------------------------
$_map(MM_RETURN_epoll_create, int);
$_map(MM_PARAMS_epoll_create, int size);
$_map(MM_ARGS_epoll_create, size);
$_map(MM_RETURN_epoll_create1, int);
$_map(MM_PARAMS_epoll_create1, int flags);
$_map(MM_ARGS_epoll_create1, flags);
$_map(MM_RETURN_epoll_ctl, int);
$_map(MM_PARAMS_epoll_ctl, int epfd, int op, int fd, struct epoll_event *event);
$_map(MM_ARGS_epoll_ctl, epfd, op, fd, event);
$_map(MM_RETURN_epoll_wait, int);
$_map(MM_PARAMS_epoll_wait, int epfd, struct epoll_event *events, int maxevents, int timeout);
$_map(MM_ARGS_epoll_wait, epfd, events, maxevents, timeout);
$_map(MM_RETURN_epoll_pwait, int);
$_map(MM_PARAMS_epoll_pwait, int epfd, struct epoll_event *events, int maxevents, int timeout, const sigset_t *sigmask);
$_map(MM_ARGS_epoll_pwait, epfd, events, maxevents, timeout, sigmask);
$_map(MM_RETURN_epoll_pwait2, int);
$_map(MM_PARAMS_epoll_pwait2, int epfd, struct epoll_event *events, int maxevents, const struct timespec *timeout, const sigset_t *sigmask);
$_map(MM_ARGS_epoll_pwait2, epfd, events, maxevents, timeout, sigmask);
$_dl; // -----------------------------------------------------------------------
$_dl; // eventfd
$_dl; // -----------------------------------------------------------------------
$_map(MM_RETURN_eventfd, int);
$_map(MM_PARAMS_eventfd, unsigned int initval, int flags);
$_map(MM_ARGS_eventfd, initval, flags);
$_dl; // -----------------------------------------------------------------------
$_dl; // fcntl
$_dl; // -----------------------------------------------------------------------
$_map(MM_RETURN_creat, int);
$_map(MM_PARAMS_creat, const char *path, mode_t mode);
$_map(MM_ARGS_creat, path, mode);
$_map(MM_RETURN_fcntl, int);
$_map(MM_PARAMS_fcntl, int fd, int cmd, ...);
$_map(MM_ARGS_fcntl, fd, cmd);
$_map(MM_RETURN_open, int);
$_map(MM_PARAMS_open, const char *path, int flags, ...);
$_map(MM_ARGS_open, path, flags);
$_map(MM_RETURN_openat, int);
$_map(MM_PARAMS_openat, int dirfd, const char *path, int flags, mode_t mode);
$_map(MM_ARGS_openat, dirfd, path, flags, mode);
$_map(MM_RETURN_openat2, int);
$_map(MM_PARAMS_openat2, int dirfd, const char *path, const struct open_how *how, size_t size);
$_map(MM_ARGS_openat2, dirfd, path, how, size);
$_map(MM_RETURN_posix_fadvise, int);
$_map(MM_PARAMS_posix_fadvise, int fd, off_t offset, off_t size, int advice);
$_map(MM_ARGS_posix_fadvise, fd, offset, size, advice);
$_map(MM_RETURN_posix_fallocate, int);
$_map(MM_PARAMS_posix_fallocate, int fd, off_t offset, off_t size);
$_map(MM_ARGS_posix_fallocate, fd, offset, size);
$_dl; // -----------------------------------------------------------------------
$_dl; // poll
$_dl; // -----------------------------------------------------------------------
$_map(MM_RETURN_poll, int);
$_map(MM_PARAMS_poll, struct pollfd *fds, nfds_t nfds, int timeout);
$_map(MM_ARGS_poll, fds, nfds, timeout);
$_dl; // -----------------------------------------------------------------------
$_dl; // random
$_dl; // -----------------------------------------------------------------------
$_map(MM_RETURN_getrandom, ssize_t);
$_map(MM_PARAMS_getrandom, void *buf, size_t size, unsigned int flags);
$_map(MM_ARGS_getrandom, buf, size, flags);
$_dl; // -----------------------------------------------------------------------
$_dl; // socket
$_dl; // -----------------------------------------------------------------------
$_map(MM_RETURN_accept, int);
$_map(MM_PARAMS_accept, int sockfd, struct sockaddr *addr, socklen_t *addrlen);
$_map(MM_ARGS_accept, sockfd, addr, addrlen);
$_map(MM_RETURN_accept4, int);
$_map(MM_PARAMS_accept4, int sockfd, struct sockaddr *addr, socklen_t *addrlen, int flags);
$_map(MM_ARGS_accept4, sockfd, addr, addrlen, flags);
$_map(MM_RETURN_bind, int);
$_map(MM_PARAMS_bind, int sockfd, const struct sockaddr *addr, socklen_t addrlen);
$_map(MM_ARGS_bind, sockfd, addr, addrlen);
$_map(MM_RETURN_connect, int);
$_map(MM_PARAMS_connect, int sockfd, const struct sockaddr *addr, socklen_t addrlen);
$_map(MM_ARGS_connect, sockfd, addr, addrlen);
$_map(MM_RETURN_getpeername, int);
$_map(MM_PARAMS_getpeername, int sockfd, struct sockaddr *addr, socklen_t *addrlen);
$_map(MM_ARGS_getpeername, sockfd, addr, addrlen);
$_map(MM_RETURN_getsockname, int);
$_map(MM_PARAMS_getsockname, int sockfd, struct sockaddr *addr, socklen_t *addrlen);
$_map(MM_ARGS_getsockname, sockfd, addr, addrlen);
$_map(MM_RETURN_getsockopt, int);
$_map(MM_PARAMS_getsockopt, int sockfd, int level, int optname, void *optval, socklen_t *optlen);
$_map(MM_ARGS_getsockopt, sockfd, level, optname, optval, optlen);
$_map(MM_RETURN_setsockopt, int);
$_map(MM_PARAMS_setsockopt, int sockfd, int level, int optname, const void *optval, socklen_t optlen);
$_map(MM_ARGS_setsockopt, sockfd, level, optname, optval, optlen);
$_map(MM_RETURN_listen, int);
$_map(MM_PARAMS_listen, int sockfd, int backlog);
$_map(MM_ARGS_listen, sockfd, backlog);
$_map(MM_RETURN_recv, ssize_t);
$_map(MM_PARAMS_recv, int sockfd, void *buf, size_t len, int flags);
$_map(MM_ARGS_recv, sockfd, buf, len, flags);
$_map(MM_RETURN_recvfrom, ssize_t);
$_map(MM_PARAMS_recvfrom, int sockfd, void *buf, size_t len, int flags, struct sockaddr *src_addr, socklen_t *addrlen);
$_map(MM_ARGS_recvfrom, sockfd, buf, len, flags, src_addr, addrlen);
$_map(MM_RETURN_recvmsg, ssize_t);
$_map(MM_PARAMS_recvmsg, int sockfd, struct msghdr *msg, int flags);
$_map(MM_ARGS_recvmsg, sockfd, msg, flags);
$_map(MM_RETURN_send, ssize_t);
$_map(MM_PARAMS_send, int sockfd, const void *buf, size_t len, int flags);
$_map(MM_ARGS_send, sockfd, buf, len, flags);
$_map(MM_RETURN_sendto, ssize_t);
$_map(MM_PARAMS_sendto, int sockfd, const void *buf, size_t len, int flags, const struct sockaddr *dest_addr, socklen_t addrlen);
$_map(MM_ARGS_sendto, sockfd, buf, len, flags, dest_addr, addrlen);
$_map(MM_RETURN_sendmsg, ssize_t);
$_map(MM_PARAMS_sendmsg, int sockfd, const struct msghdr *msg, int flags);
$_map(MM_ARGS_sendmsg, sockfd, msg, flags);
$_map(MM_RETURN_shutdown, int);
$_map(MM_PARAMS_shutdown, int sockfd, int how);
$_map(MM_ARGS_shutdown, sockfd, how);
$_map(MM_RETURN_socket, int);
$_map(MM_PARAMS_socket, int domain, int type, int protocol);
$_map(MM_ARGS_socket, domain, type, protocol);
$_map(MM_RETURN_socketpair, int);
$_map(MM_PARAMS_socketpair, int domain, int type, int protocol, int sv[2]);
$_map(MM_ARGS_socketpair, domain, type, protocol, sv);
$_dl; // -----------------------------------------------------------------------
$_dl; // time
$_dl; // -----------------------------------------------------------------------
$_map(MM_RETURN_asctime, char *);
$_map(MM_PARAMS_asctime, const struct tm *timeptr);
$_map(MM_ARGS_asctime, timeptr);
$_map(MM_RETURN_asctime_r, char *);
$_map(MM_PARAMS_asctime_r, const struct tm *tm, char *buf);
$_map(MM_ARGS_asctime_r, tm, buf);
$_map(MM_RETURN_clock_getres, int);
$_map(MM_PARAMS_clock_getres, clockid_t clockid, struct timespec *res);
$_map(MM_ARGS_clock_getres, clockid, res);
$_map(MM_RETURN_clock_gettime, int);
$_map(MM_PARAMS_clock_gettime, clockid_t clockid, struct timespec *tp);
$_map(MM_ARGS_clock_gettime, clockid, tp);
$_map(MM_RETURN_clock_settime, int);
$_map(MM_PARAMS_clock_settime, clockid_t clockid, const struct timespec *tp);
$_map(MM_ARGS_clock_settime, clockid, tp);
$_map(MM_RETURN_ctime, char *);
$_map(MM_PARAMS_ctime, const time_t *timep);
$_map(MM_ARGS_ctime, timep);
$_map(MM_RETURN_ctime_r, char *);
$_map(MM_PARAMS_ctime_r, const time_t *timep, char *buf);
$_map(MM_ARGS_ctime_r, timep, buf);
$_map(MM_RETURN_difftime, double);
$_map(MM_PARAMS_difftime, time_t time1, time_t time0);
$_map(MM_ARGS_difftime, time1, time0);
$_map(MM_RETURN_getdate, struct tm *);
$_map(MM_PARAMS_getdate, const char *string);
$_map(MM_ARGS_getdate, string);
$_map(MM_RETURN_getdate_r, int);
$_map(MM_PARAMS_getdate_r, const char *string, struct tm *res);
$_map(MM_ARGS_getdate_r, string, res);
$_map(MM_RETURN_gmtime, struct tm *);
$_map(MM_PARAMS_gmtime, const time_t *timep);
$_map(MM_ARGS_gmtime, timep);
$_map(MM_RETURN_gmtime_r, struct tm *);
$_map(MM_PARAMS_gmtime_r, const time_t *timep, struct tm *result);
$_map(MM_ARGS_gmtime_r, timep, result);
$_map(MM_RETURN_localtime, struct tm *);
$_map(MM_PARAMS_localtime, const time_t *timep);
$_map(MM_ARGS_localtime, timep);
$_map(MM_RETURN_localtime_r, struct tm *);
$_map(MM_PARAMS_localtime_r, const time_t *timep, struct tm *result);
$_map(MM_ARGS_localtime_r, timep, result);
$_map(MM_RETURN_mktime, time_t);
$_map(MM_PARAMS_mktime, struct tm *tm);
$_map(MM_ARGS_mktime, tm);
$_map(MM_RETURN_nanosleep, int);
$_map(MM_PARAMS_nanosleep, const struct timespec *duration, struct timespec *rem);
$_map(MM_ARGS_nanosleep, duration, rem);
$_map(MM_RETURN_strftime, size_t);
$_map(MM_PARAMS_strftime, char *s, size_t max, const char *format, const struct tm *tm);
$_map(MM_ARGS_strftime, s, max, format, tm);
$_map(MM_RETURN_strftime_l, size_t);
$_map(MM_PARAMS_strftime_l, char *s, size_t max, const char *format, const struct tm *tm, locale_t locale);
$_map(MM_ARGS_strftime_l, s, max, format, tm, locale);
$_map(MM_RETURN_strptime, char *);
$_map(MM_PARAMS_strptime, const char *s, const char *format, struct tm *tm);
$_map(MM_ARGS_strptime, s, format, tm);
$_map(MM_RETURN_time, time_t);
$_map(MM_PARAMS_time, time_t *tloc);
$_map(MM_ARGS_time, tloc);
$_map(MM_RETURN_timer_create, int);
$_map(MM_PARAMS_timer_create, clockid_t clockid, struct sigevent *sevp, timer_t *timerid);
$_map(MM_ARGS_timer_create, clockid, sevp, timerid);
$_map(MM_RETURN_timer_delete, int);
$_map(MM_PARAMS_timer_delete, timer_t timerid);
$_map(MM_ARGS_timer_delete, timerid);
$_map(MM_RETURN_timer_gettime, int);
$_map(MM_PARAMS_timer_gettime, timer_t timerid, struct itimerspec *curr_value);
$_map(MM_ARGS_timer_gettime, timerid, curr_value);
$_map(MM_RETURN_timer_getoverrun, int);
$_map(MM_PARAMS_timer_getoverrun, timer_t timerid);
$_map(MM_ARGS_timer_getoverrun, timerid);
$_map(MM_RETURN_timer_settime, int);
$_map(MM_PARAMS_timer_settime, timer_t timerid, int flags, const struct itimerspec *new_value, struct itimerspec *old_value);
$_map(MM_ARGS_timer_settime, timerid, flags, new_value, old_value);
$_map(MM_RETURN_getitimer, int);
$_map(MM_PARAMS_getitimer, __itimer_which_t which, struct itimerval *curr_value);
$_map(MM_ARGS_getitimer, which, curr_value);
$_map(MM_RETURN_setitimer, int);
$_map(MM_PARAMS_setitimer, __itimer_which_t which, const struct itimerval *new_value, struct itimerval *old_value);
$_map(MM_ARGS_setitimer, which, new_value, old_value);
$_map(MM_RETURN_gettimeofday, int);
$_map(MM_PARAMS_gettimeofday, struct timeval *tv, void *tz);
$_map(MM_ARGS_gettimeofday, tv, tz);
$_map(MM_RETURN_settimeofday, int);
$_map(MM_PARAMS_settimeofday, const struct timeval *tv, const struct timezone *tz);
$_map(MM_ARGS_settimeofday, tv, tz);
$_map(MM_RETURN_select, int);
$_map(MM_PARAMS_select, int nfds, fd_set *readfds, fd_set *writefds, fd_set *exceptfds, struct timeval *timeout);
$_map(MM_ARGS_select, nfds, readfds, writefds, exceptfds, timeout);
$_map(MM_RETURN_utimes, int);
$_map(MM_PARAMS_utimes, const char *path, const struct timeval times[2]);
$_map(MM_ARGS_utimes, path, times);
$_dl; // -----------------------------------------------------------------------
$_dl; // uio
$_dl; // -----------------------------------------------------------------------
$_map(MM_RETURN_readv, ssize_t);
$_map(MM_PARAMS_readv, int fd, const struct iovec *iov, int iovcnt);
$_map(MM_ARGS_readv, fd, iov, iovcnt);
$_map(MM_RETURN_writev, ssize_t);
$_map(MM_PARAMS_writev, int fd, const struct iovec *iov, int iovcnt);
$_map(MM_ARGS_writev, fd, iov, iovcnt);
$_dl; // -----------------------------------------------------------------------
$_dl; // unistd
$_dl; // -----------------------------------------------------------------------
$_map(MM_RETURN_access, int);
$_map(MM_PARAMS_access, const char *path, int mode);
$_map(MM_ARGS_access, path, mode);
$_map(MM_RETURN_alarm, unsigned int);
$_map(MM_PARAMS_alarm, unsigned int seconds);
$_map(MM_ARGS_alarm, seconds);
$_map(MM_RETURN_brk, int);
$_map(MM_PARAMS_brk, void *addr);
$_map(MM_ARGS_brk, addr);
$_map(MM_RETURN_chdir, int);
$_map(MM_PARAMS_chdir, const char *path);
$_map(MM_ARGS_chdir, path);
$_map(MM_RETURN_chroot, int);
$_map(MM_PARAMS_chroot, const char *path);
$_map(MM_ARGS_chroot, path);
$_map(MM_RETURN_chown, int);
$_map(MM_PARAMS_chown, const char *path, uid_t owner, gid_t group);
$_map(MM_ARGS_chown, path, owner, group);
$_map(MM_RETURN_close, int);
$_map(MM_PARAMS_close, int fd);
$_map(MM_ARGS_close, fd);
$_map(MM_RETURN_confstr, size_t);
$_map(MM_PARAMS_confstr, int name, char *buf, size_t size);
$_map(MM_ARGS_confstr, name, buf, size);
$_map(MM_RETURN_crypt, char *);
$_map(MM_PARAMS_crypt, const char *key, const char *salt);
$_map(MM_ARGS_crypt, key, salt);
$_map(MM_RETURN_ctermid, char *);
$_map(MM_PARAMS_ctermid, char *s);
$_map(MM_ARGS_ctermid, s);
$_map(MM_RETURN_cuserid, char *);
$_map(MM_PARAMS_cuserid, char *string);
$_map(MM_ARGS_cuserid, string);
$_map(MM_RETURN_dup, int);
$_map(MM_PARAMS_dup, int oldfd);
$_map(MM_ARGS_dup, oldfd);
$_map(MM_RETURN_dup2, int);
$_map(MM_PARAMS_dup2, int oldfd, int newfd);
$_map(MM_ARGS_dup2, oldfd, newfd);
$_map(MM_RETURN_dup3, int);
$_map(MM_PARAMS_dup3, int oldfd, int newfd, int flags);
$_map(MM_ARGS_dup3, oldfd, newfd, flags);
$_map(MM_RETURN_faccessat, int);
$_map(MM_PARAMS_faccessat, int dirfd, const char *path, int mode, int flags);
$_map(MM_ARGS_faccessat, dirfd, path, mode, flags);
$_map(MM_RETURN_fchdir, int);
$_map(MM_PARAMS_fchdir, int fd);
$_map(MM_ARGS_fchdir, fd);
$_map(MM_RETURN_fchown, int);
$_map(MM_PARAMS_fchown, int fildes, uid_t owner, gid_t group);
$_map(MM_ARGS_fchown, fildes, owner, group);
$_map(MM_RETURN_fchownat, int);
$_map(MM_PARAMS_fchownat, int fd, const char *path, uid_t owner, gid_t group, int flag);
$_map(MM_ARGS_fchownat, fd, path, owner, group, flag);
$_map(MM_RETURN_fdatasync, int);
$_map(MM_PARAMS_fdatasync, int fd);
$_map(MM_ARGS_fdatasync, fd);
$_map(MM_RETURN_fpathconf, long);
$_map(MM_PARAMS_fpathconf, int fd, int name);
$_map(MM_ARGS_fpathconf, fd, name);
$_map(MM_RETURN_fsync, int);
$_map(MM_PARAMS_fsync, int fd);
$_map(MM_ARGS_fsync, fd);
$_map(MM_RETURN_ftruncate, int);
$_map(MM_PARAMS_ftruncate, int fildes, off_t length);
$_map(MM_ARGS_ftruncate, fildes, length);
$_map(MM_RETURN_getcwd, char *);
$_map(MM_PARAMS_getcwd, char *buf, size_t size);
$_map(MM_ARGS_getcwd, buf, size);
$_map(MM_RETURN_getgroups, int);
$_map(MM_PARAMS_getgroups, int size, gid_t list[]);
$_map(MM_ARGS_getgroups, size, list);
$_map(MM_RETURN_getlogin_r, int);
$_map(MM_PARAMS_getlogin_r, char *buf, size_t bufsize);
$_map(MM_ARGS_getlogin_r, buf, bufsize);
$_map(MM_RETURN_getopt, int);
$_map(MM_PARAMS_getopt, int argc, char * const *argv, const char *optstring);
$_map(MM_ARGS_getopt, argc, argv, optstring);
$_map(MM_RETURN_getpass, char *);
$_map(MM_PARAMS_getpass, const char *prompt);
$_map(MM_ARGS_getpass, prompt);
$_map(MM_RETURN_getpgid, pid_t);
$_map(MM_PARAMS_getpgid, pid_t pid);
$_map(MM_ARGS_getpgid, pid);
$_map(MM_RETURN_getsid, pid_t);
$_map(MM_PARAMS_getsid, pid_t pid);
$_map(MM_ARGS_getsid, pid);
$_map(MM_RETURN_getwd, char *);
$_map(MM_PARAMS_getwd, char *buf);
$_map(MM_ARGS_getwd, buf);
$_map(MM_RETURN_isatty, int);
$_map(MM_PARAMS_isatty, int fd);
$_map(MM_ARGS_isatty, fd);
$_map(MM_RETURN_lchown, int);
$_map(MM_PARAMS_lchown, const char *path, uid_t owner, gid_t group);
$_map(MM_ARGS_lchown, path, owner, group);
$_map(MM_RETURN_link, int);
$_map(MM_PARAMS_link, const char *oldpath, const char *newpath);
$_map(MM_ARGS_link, oldpath, newpath);
$_map(MM_RETURN_linkat, int);
$_map(MM_PARAMS_linkat, int olddirfd, const char *oldpath, int newdirfd, const char *newpath, int flags);
$_map(MM_ARGS_linkat, olddirfd, oldpath, newdirfd, newpath, flags);
$_map(MM_RETURN_lockf, int);
$_map(MM_PARAMS_lockf, int fd, int op, off_t size);
$_map(MM_ARGS_lockf, fd, op, size);
$_map(MM_RETURN_lseek, off_t);
$_map(MM_PARAMS_lseek, int fd, off_t offset, int whence);
$_map(MM_ARGS_lseek, fd, offset, whence);
$_map(MM_RETURN_nice, int);
$_map(MM_PARAMS_nice, int inc);
$_map(MM_ARGS_nice, inc);
$_map(MM_RETURN_pathconf, long);
$_map(MM_PARAMS_pathconf, const char *path, int name);
$_map(MM_ARGS_pathconf, path, name);
$_map(MM_RETURN_pipe, int);
$_map(MM_PARAMS_pipe, int pipefd[2]);
$_map(MM_ARGS_pipe, pipefd);
$_map(MM_RETURN_pipe2, int);
$_map(MM_PARAMS_pipe2, int pipefd[2], int flags);
$_map(MM_ARGS_pipe2, pipefd, flags);
$_map(MM_RETURN_pread, ssize_t);
$_map(MM_PARAMS_pread, int fd, void *buf, size_t count, off_t offset);
$_map(MM_ARGS_pread, fd, buf, count, offset);
$_map(MM_RETURN_pthread_atfork, int);
$_map(MM_PARAMS_pthread_atfork, void (*prepare)(void), void (*parent)(void), void (*child)(void));
$_map(MM_ARGS_pthread_atfork, prepare, parent, child);
$_map(MM_RETURN_pwrite, ssize_t);
$_map(MM_PARAMS_pwrite, int fd, const void *buf, size_t count, off_t offset);
$_map(MM_ARGS_pwrite, fd, buf, count, offset);
$_map(MM_RETURN_read, ssize_t);
$_map(MM_PARAMS_read, int fd, void *buf, size_t count);
$_map(MM_ARGS_read, fd, buf, count);
$_map(MM_RETURN_readlink, ssize_t);
$_map(MM_PARAMS_readlink, const char *path, char *buf, size_t bufsiz);
$_map(MM_ARGS_readlink, path, buf, bufsiz);
$_map(MM_RETURN_readlinkat, ssize_t);
$_map(MM_PARAMS_readlinkat, int dirfd, const char *path, char *buf, size_t bufsiz);
$_map(MM_ARGS_readlinkat, dirfd, path, buf, bufsiz);
$_map(MM_RETURN_rmdir, int);
$_map(MM_PARAMS_rmdir, const char *path);
$_map(MM_ARGS_rmdir, path);
$_map(MM_RETURN_sbrk, void *);
$_map(MM_PARAMS_sbrk, intptr_t increment);
$_map(MM_ARGS_sbrk, increment);
$_map(MM_RETURN_setgid, int);
$_map(MM_PARAMS_setgid, gid_t gid);
$_map(MM_ARGS_setgid, gid);
$_map(MM_RETURN_sethostid, int);
$_map(MM_PARAMS_sethostid, long hostid);
$_map(MM_ARGS_sethostid, hostid);
$_map(MM_RETURN_setpgid, int);
$_map(MM_PARAMS_setpgid, pid_t pid, pid_t pgid);
$_map(MM_ARGS_setpgid, pid, pgid);
$_map(MM_RETURN_setregid, int);
$_map(MM_PARAMS_setregid, gid_t rgid, gid_t egid);
$_map(MM_ARGS_setregid, rgid, egid);
$_map(MM_RETURN_setreuid, int);
$_map(MM_PARAMS_setreuid, uid_t ruid, uid_t euid);
$_map(MM_ARGS_setreuid, ruid, euid);
$_map(MM_RETURN_setuid, int);
$_map(MM_PARAMS_setuid, uid_t uid);
$_map(MM_ARGS_setuid, uid);
$_map(MM_RETURN_sleep, unsigned int);
$_map(MM_PARAMS_sleep, unsigned int seconds);
$_map(MM_ARGS_sleep, seconds);
$_map(MM_RETURN_swab, void);
$_map(MM_PARAMS_swab, const void *from, void *to, ssize_t n);
$_map(MM_ARGS_swab, from, to, n);
$_map(MM_RETURN_symlink, int);
$_map(MM_PARAMS_symlink, const char *target, const char *linkpath);
$_map(MM_ARGS_symlink, target, linkpath);
$_map(MM_RETURN_symlinkat, int);
$_map(MM_PARAMS_symlinkat, const char *target, int newdirfd, const char *linkpath);
$_map(MM_ARGS_symlinkat, target, newdirfd, linkpath);
$_map(MM_RETURN_syncfs, int);
$_map(MM_PARAMS_syncfs, int fd);
$_map(MM_ARGS_syncfs, fd);
$_map(MM_RETURN_sysconf, long);
$_map(MM_PARAMS_sysconf, int name);
$_map(MM_ARGS_sysconf, name);
$_map(MM_RETURN_tcgetpgrp, pid_t);
$_map(MM_PARAMS_tcgetpgrp, int fd);
$_map(MM_ARGS_tcgetpgrp, fd);
$_map(MM_RETURN_tcsetpgrp, int);
$_map(MM_PARAMS_tcsetpgrp, int fd, pid_t pgrp);
$_map(MM_ARGS_tcsetpgrp, fd, pgrp);
$_map(MM_RETURN_truncate, int);
$_map(MM_PARAMS_truncate, const char *path, off_t length);
$_map(MM_ARGS_truncate, path, length);
$_map(MM_RETURN_ttyname, char *);
$_map(MM_PARAMS_ttyname, int fd);
$_map(MM_ARGS_ttyname, fd);
$_map(MM_RETURN_ttyname_r, int);
$_map(MM_PARAMS_ttyname_r, int fd, char *buf, size_t size);
$_map(MM_ARGS_ttyname_r, fd, buf, size);
$_map(MM_RETURN_ualarm, useconds_t);
$_map(MM_PARAMS_ualarm, useconds_t usecs, useconds_t interval);
$_map(MM_ARGS_ualarm, usecs, interval);
$_map(MM_RETURN_unlink, int);
$_map(MM_PARAMS_unlink, const char *path);
$_map(MM_ARGS_unlink, path);
$_map(MM_RETURN_unlinkat, int);
$_map(MM_PARAMS_unlinkat, int dirfd, const char *path, int flags);
$_map(MM_ARGS_unlinkat, dirfd, path, flags);
$_map(MM_RETURN_usleep, int);
$_map(MM_PARAMS_usleep, useconds_t usec);
$_map(MM_ARGS_usleep, usec);
$_map(MM_RETURN_write, ssize_t);
$_map(MM_PARAMS_write, int fd, const void *buf, size_t count);
$_map(MM_ARGS_write, fd, buf, count);
